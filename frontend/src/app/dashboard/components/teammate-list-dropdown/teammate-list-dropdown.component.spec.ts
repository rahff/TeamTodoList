import { TeammateListDropdownComponent } from './teammate-list-dropdown.component';



describe('DropdownMenuComponent', () => {
  let component: TeammateListDropdownComponent;

  beforeEach(() => {
    component = new TeammateListDropdownComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
