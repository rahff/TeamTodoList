import { TeammatesComponent } from './teammates.component';



describe('TeammatesComponent', () => {
  let component: TeammatesComponent;

  beforeEach(() => {
    component = new TeammatesComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
